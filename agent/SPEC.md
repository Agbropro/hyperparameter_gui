# REQUEST

GUI to find the best hyperparameter from training yolo model. It has the option to train detection segmentation or classification model. It finds the train and test folder for the training purposes. It creates random yaml using all yolo hyperparameter, it chooses what evaluation metrics it wants to improve which metrics like precision, recall, f1-score, mAP score or anything else. The training will run for n trials and y epochs, it then calculates which hyperparameter from each trial produce the best metrics we want to improve. i want it to use clean architecture and use simple frontend with ui ux in the frontend folder. let all code be modular and simple to understand, utilizing my current skeleton architecture. remember no ARGPARSE please

discuss with me about this and the project viability, ask your question in the #QUESTION section below

# QUESTION